# ================================================================
# UNIVERSITY MANAGEMENT SYSTEM - SIMPLIFIED CONFIGURATION
# ================================================================

# Active Profile (production by default for deployment)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/api

# MongoDB Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://gogurlamanoj722:ybeWTEjVmM9Mkhuu@university-project.1kftdns.mongodb.net/University?retryWrites=true&w=majority&appName=University-Project}
spring.data.mongodb.database=${MONGODB_DATABASE:University}

# JWT Configuration
jwt.secret=${JWT_SECRET:Tech_Mahindra_COE_Project_JWT_Secret_Key_2025_University_Management_System}
jwt.expiration=${JWT_EXPIRATION:604800000}

# CORS Configuration
cors.allowed.origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:5174,https://university-frontend-tau.vercel.app}

# Logging Configuration
logging.level.com.university.management=${LOG_LEVEL:INFO}
logging.level.org.springframework.security=${SECURITY_LOG_LEVEL:WARN}

# Application Information
app.name=University Management System
app.version=1.0.0
